import { collect } from "collect.js";

export function c(arr: any) {
  return collect(arr);
}


// const hello = (arr: any) => {
//   collect(arr);
// };
// declare function $hello(): void;
// module.exports = hello;